apiVersion: apps/v1
kind: DaemonSet
metadata:
  generateName: fake-ds-to-restart-
  labels:
    app: fake-ds-to-restart
spec:
  selector:
    matchLabels:
      app: fake-ds-to-restart
  template:
    metadata:
      labels:
        app: fake-ds-to-restart
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: type
                    operator: In
                    values:
                      - kwok
      tolerations:
        - key: "kwok.x-k8s.io/node"
          operator: "Exists"
          effect: "NoSchedule"
      containers:
        - name: fake-container
          image: fake-image