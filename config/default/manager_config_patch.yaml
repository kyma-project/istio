apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
  labels:
    sidecar.istio.io/inject: "false"
spec:
  template:
    spec:
      containers:
      - name: manager
