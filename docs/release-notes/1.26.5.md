## Enhancements

- You can now configure alpha Istio features using the `istio-features` ConfigMap. Currently, the only available feature is `disableCni`. Alpha Istio features are not recommended for production environments.
